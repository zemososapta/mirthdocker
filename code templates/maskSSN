/**
	This function mask last four digit of ssn
	@param {String} ssn - Nine digit social security number
	@return {String} return Masked ssn
*/
function maskSSN(ssn) {
	var length = ssn.length();

    if (length != 9) 
    	return '' ;
    else
    	return ssn.substring(0, length - 4) + "****";
}
